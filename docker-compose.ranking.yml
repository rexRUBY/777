version: '3.8'

services:
  batch:
    build:
      context: .
      dockerfile: ./batch/Dockerfile
    image: ${RANKING_REPOSITORY_URI}:latest
    ports:
      - "8082:8082"
    environment:
      DB_HOST: ${DB_HOST}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}